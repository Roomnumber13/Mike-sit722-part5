apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-catalog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: book-catalog
  template:
    metadata:
      labels:
        app: book-catalog
    spec:
      containers:
      - name: book-catalog
        image: $CONTAINER_REGISTRY/book-catalog:$VERSION
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://m224246429_user:kFXWFcBhlWt8poRTVgJJMnitxoSgXaIc@dpg-crna0c68ii6s73en725g-a.singapore-postgres.render.com/m224246429"

---

apiVersion: v1
kind: Service
metadata:
  name: book-catalog-service
spec:
  selector:
    app: book-catalog
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: LoadBalancer

